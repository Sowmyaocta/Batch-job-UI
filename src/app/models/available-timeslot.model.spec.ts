import { AvailableTimeslot } from './available-timeslot.model';

describe('AvailableTimesloty', () => {
  it('should create an instance', () => {
    expect(new AvailableTimeslot()).toBeTruthy();
  });
});
